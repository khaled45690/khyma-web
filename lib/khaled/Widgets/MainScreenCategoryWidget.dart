import 'package:flutter/material.dart';

class MainScreenCategoryWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return InkWell(
      child: Column(
        children: [
          Container(

          ),
        ],
      ),
    );
  }
}
